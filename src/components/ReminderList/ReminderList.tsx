import { FC } from "react";

import { Center, Container } from "../../../styled-system/jsx";

export const ReminderList: FC = () => {
  return (
    <Container>
      <Center>
        <h1>ReminderList</h1>
      </Center>
    </Container>
  );
};
